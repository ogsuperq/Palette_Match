import { SLIDES_A } from "./SlidesA";
import { SLIDES_B } from "./SlidesB";

// 13 slide components in order. The last (Close) accepts an onDownload prop.
export const SLIDES = [...SLIDES_A, ...SLIDES_B];
